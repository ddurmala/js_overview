var firstName = 'Dana'; //string
var lastName = 'Castagna';
var age = 39; //Number
var isADev = true; //Boolean
var un = undefined; //Undefined - provided by JS
var n = null; //programmer's representation of nothing
var notANumber = NaN; //Not a Number  - this is provided when u try to process a mathematical equation and the result is not a valid # // provided by JS

var nothing;

//age = age + 1;

//console.log(age);

// firstName + 'Castagna';

var fullName = firstName + ' ' + lastName;

// console.log(fullName);

var names = ['dana', 'nick', 'julie', 'justine', 'kevin', 'lynn'];
var ages = [30, 19, 22, 45]


names.push('susan');

names.unshift('pete');

names.pop();


console.log(names);


//console.log(names[names.length - 1]);

//names[1] = 'joe';

//console.log(names);

//ages[2] += 10;

//console.log(ages);

